import React from 'react'
import { logoDark } from '../utils'

const Navbar = () => {
  return (
    <header>
        <nav>
            <img src={logoDark} alt="logo" />
        </nav>
    </header>
  )
}

export default Navbar
